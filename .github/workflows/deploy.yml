name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '14'

      - name: Install dependencies
        run: npm install

      - name: Build
        run: npm run build

      - name: Deploy to GitHub Pages
        run: |
          git config --global user.email "ps4486105@gmail.com"
          git config --global user.name "JenaPartha"
          git remote add origin_with_token "https://JenaPartha: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@github.com/JenaPartha/my-react-app.git"
          git push origin_with_token HEAD:gh-pages
